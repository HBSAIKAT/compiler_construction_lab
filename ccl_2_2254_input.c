// Write a program to detect comment from a C program and remove in the output file.
#include <stdio.h>

int main() {
    int a, b, sum, product;
// input first number
    printf("Enter first number: ");
    scanf("%d", &a);
// input second number
    printf("Enter second number: ");
    scanf("%d", &b);
// Sum
    sum = a + b;
    product = a * b;

/*Final 
Result
*/
    printf("Sum: %d\n", sum);
    printf("Product: %d\n", product);

    return 0;
}
