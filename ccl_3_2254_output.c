#include <stdio.h>

int main() {
    int a, b, sum, product;

    printf("Enter first number: ");
    scanf("%d", &a);

    printf("Enter second number: ");
    scanf("%d", &b);



  if(a == 0 && b == 0) return 0;

    sum = a + b;
    product = a * b;

    printf("Sum: %d\n", sum);
    printf("Product: %d\n", product);

    return 0;

 print(" Here result will be come in int number")
}